<div class="text-center">
  <%= form_with url: define_path, class: "relative inline-block" do |form| %>
    <i class="fas fa-search absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
    <%= form.text_field :term, class: "border border-gray-300 rounded-lg py-2 pl-10 pr-4 w-full max-w-md text-lg md:text-xl font-serif shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500", placeholder: "Define a term…", required: true %>
  <% end %>
</div>
